plugins {
    id 'java-library'
    id 'org.ceskaexpedice.processplatform.process' version '1.0.6'
}

description "Delete tree"



processPlugin {
	spiImplementation='org.kramerius.plugin.DeleteTreeSPI'
    profiles = [ [profileId: "delete_tree",
                  description: "Delete object tree from akubra repo - legacy name of process",
                  jvmArgs: ["-Xms1g","-Xmx16g"]] ]

	excludeDependencies = [
        'com.sun.jersey'
    ]
}

dependencies{
    implementation project(':processes:dnntflag')
    implementation "org.ceskaexpedice:process-api:${processapiversion}"
}


